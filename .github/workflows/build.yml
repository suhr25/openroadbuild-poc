name: OpenROAD Build POC

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run build script
        run: |
          chmod +x build.sh
          ./build.sh
